
---

### File 2: `workflow.json`
**Click "Add file" -> "Create new file" -> Name it `workflow.json`**
*(Note: This is a simplified version of the JSON for the prompt. In a real n8n instance, you would click "Export" and paste that code here.)*

```json
{
  "nodes": [
    {
      "parameters": { "rule": { "interval": [{ "field": "hours", "interval": 8 }] } },
      "name": "Schedule Trigger",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1,
      "position": [100, 300]
    },
    {
      "parameters": {
        "conditions": {
          "dateTime": [{
            "value1": "={{ $json.postedOn }}",
            "value2": "={{ $now.minus(6, 'hours') }}",
            "operator": "after"
          }]
        }
      },
      "name": "Filter: 6 Hours",
      "type": "n8n-nodes-base.filter",
      "typeVersion": 1,
      "position": [400, 300]
    },
    {
      "parameters": {
        "model": "gpt-4o",
        "messages": {
          "messageValues": [{
            "role": "system",
            "content": "Score this Upwork job 1-10 based on n8n and AI automation relevance. Output JSON: {\"score\": number, \"priority\": \"High/Medium/Low\", \"reasoning\": \"string\"}"
          }]
        }
      },
      "name": "OpenAI Scorer",
      "type": "n8n-nodes-base.openAi",
      "typeVersion": 1,
      "position": [600, 300]
    }
  ]
}
